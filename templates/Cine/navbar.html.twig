{% set current_path = app.request.get('_route')|split('_') %}
<nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-dark">
   <div class="container-fluid w-100">
       <div class="row align-items-center text-white w-100">
           <div class="col-auto">
               <a class="navbar-brand" style="font-size: 3rem; font-weight: bold" href="{{ path('mainApp') }}">ETYH</a>
           </div>
           <div class="col">
               <div class="collapse navbar-collapse" id="navbarNav">
                   <ul class="navbar-nav">
                       <li class="nav-item">
                           <a class="nav-link" href="{{path('cine_main')}}">Inicio</a>
                       </li>
                       <li class="nav-item">
                           <a class="nav-link" href="{{path('cine_section1')}}">Sección 1</a>
                       </li>
                       <li class="nav-item">
                           <a class="nav-link" href="{{path('cine_section2')}}">Sección 2</a>
                       </li>
                       <li class="nav-item">
                           <a class="nav-link" href="{{path('cine_section3')}}">Sección 3</a>
                       </li>
                   </ul>
               </div>
           </div>
           <div class="col-auto">
               {# {% if app.user %}
                   <ul class="navbar-nav ml-auto">
                       <li class="nav-item">
                           <a class="nav-link {{ current_path[0] == 'principal' ? 'active" aria-current="page' }} text-white"
                           href="{{ path('mainApp') }}">Conectado como {{ app.user.userName }}</a>
                       </li>
                       <li class="nav-item">
                           <a href="{{ path('logout_App') }}" class="btn btn-secondary">Log out <i class="ti ti-user-off"></i></a>
                      
                       </li>
                   </ul>
               {% endif %} #}
           </div>
       </div>
   </div>
</nav>
